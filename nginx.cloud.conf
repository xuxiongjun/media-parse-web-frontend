server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 云端前后端分离：API 由前端 VITE_API_BASE 直连后端，此处只托管静态资源
    location / {
        try_files $uri $uri/ /index.html;
    }
}
